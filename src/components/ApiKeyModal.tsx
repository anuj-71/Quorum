/**
 * ApiKeyModal — REMOVED
 *
 * The API key is now managed server-side only.
 * GEMINI_API_KEY lives exclusively in server/.env (gitignored).
 * This file is kept as a stub to prevent import errors during transition.
 * It exports nothing and renders nothing.
 */

export {};
